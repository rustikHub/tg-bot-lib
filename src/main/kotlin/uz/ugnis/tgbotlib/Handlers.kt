package uz.ugnis.tgbotlib

import org.springframework.stereotype.Service
import org.telegram.telegrambots.meta.api.objects.CallbackQuery
import org.telegram.telegrambots.meta.api.objects.Message
import org.telegram.telegrambots.meta.bots.AbsSender
/*

@Service
class PassportInfoCreationMessageHandler :
    MessageHandler {

    override val messageHandler: (_message: Message, _sender: AbsSender) -> Unit = { message: Message, sender: AbsSender ->

    }



    override fun steps(): List<String> {
       return listOf("S")
    }
}

@Service
class AttemptCallbackHandler() : CallbackHandler {
    override val callbackHandle: (_callback: CallbackQuery, _sender: AbsSender) -> Unit =
        { callback: CallbackQuery, sender: AbsSender ->

        }

    override fun callbackTypes(): List<String> {
        return listOf("S")

    }
}
*/
